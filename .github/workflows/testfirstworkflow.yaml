name: mytestworkflow

on: push

jobs:
  test_job1:
    runs-on: ubuntu-latest
    steps: 
      - name: checkout
        uses: actions/checkout@v4

      - name: welcome
        run: echo "welcome to github action"

      - name: commands
        run: |
          ls -ltra
          cat README.md
          
