name: mytestworkflow

on: push

jobs:
  test_job1:
    runs-on: ubuntu-latest
    steps: 
      - name: checkout
        uses: actions/checkout@v4

      - name: install libraries
        run: sudo apt-get install cowsay -y

      - name: execute commands
        run: cowsay "run for cow image" >> cow.txt

      - name: Test file
        run: grep -i "cow.txt"      

      - name: commands
        run: |
          ls -ltra
          cat README.md
          
